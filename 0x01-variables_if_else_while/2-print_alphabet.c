#include <stdio.h>

/**
 * main - all codes are in main function
 *
 * Return: (0) is regarded as a success
 */

int main(void)
{
	char j;

	for (j = 'a' ; j <= 'z' ; j++)
		putchar(j);
	putchar('\n');
	return (0);


}
