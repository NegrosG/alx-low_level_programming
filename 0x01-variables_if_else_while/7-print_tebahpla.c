#include <stdio.h>

/**
 * main - all codes are in main function
 *
 * Return: (0) is regarded as a success
 */

int main(void)
{
	char k;

	for (k = 'z' ; k >= 'a' ;  k--)
		putchar(k);
	putchar('\n');
		return (0);
}
