#include "main.h"
#include <stdio.h>

/**
 * mul - thi nmultiplies two integers
 * @a: first parameter
 * @b: second parameter
 * Return: (0)
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	printf("%d of mul", mul);
	return (0);
}
